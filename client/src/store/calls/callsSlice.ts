import { createSlice } from "@reduxjs/toolkit";

interface CallState {
    
}

const callSlice = createSlice({
    name: 'calls',
    initialState: {},
    reducers: {}
});

export default callSlice.reducer;